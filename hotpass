#!/usr/bin/env bash
# Hotpass CLI launcher

# Set the Python path to include apps
dir="$(dirname "$0")"
export PYTHONPATH="$dir/apps/data-platform:${PYTHONPATH}"

# Run the CLI
exec python -u -m hotpass.cli.main "$@"
