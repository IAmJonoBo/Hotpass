#!/usr/bin/env bash
# Hotpass CLI launcher

# Set the Python path to include src
dir="$(dirname "$0")"
export PYTHONPATH="$dir/src:${PYTHONPATH}"

# Run the CLI
exec python -u "$(dirname "$0")/src/hotpass/cli.py" "$@"
