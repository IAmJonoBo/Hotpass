prefect-version: "3.4.7"
name: "hotpass"
description: |
  Deployment manifests live under ./prefect/*.yaml. Use `uv run hotpass deploy`
  to apply them. The manifests encode schedules, tags, work pools, and parameter
  defaults for refinement and backfill flows.
